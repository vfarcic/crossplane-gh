apiVersion: devopstoolkitseries.com/v1alpha1
kind: GitHubClaim
metadata:
  name: my-repo
spec:
  id: my-repo
  parameters:
    public: true
    registry: my-registry/me
    app:
      language: go
      host: my-repo.acme.com
      namespace: a-team
    repo:
      user: my-user
